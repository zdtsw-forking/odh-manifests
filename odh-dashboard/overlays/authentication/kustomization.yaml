# This is kept for backwards compatibility with existing deployments that enabled the authentication overlay
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
